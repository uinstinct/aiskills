---
name: example-skill
description: Placeholder skill used by the instinctagents registry. Replace with a real skill body before relying on it.
---

# example-skill

This file is a placeholder so the registry has at least one entry in `skills/` from day one.

When a maintainer ingests a real skill via `/skill-add`, it lands in its own folder under `skills/` with a manifest of its own. This example is safe to leave in place or delete once real skills exist.
